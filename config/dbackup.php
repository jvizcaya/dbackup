<?php

return [

    /*
     |--------------------------------------------------------------------------
     | Dbackup Settings
     |--------------------------------------------------------------------------
     */

     /*
     |--------------------------------------------------------------------------
     | Filesystem disk storage backups
     |--------------------------------------------------------------------------
    */
    'disk' => [
        'driver' => 'local',
        'root' => env('DBACKUP_PATH', storage_path('app/backup'))
    ],

    /*
    |--------------------------------------------------------------------------
    | The time in days that the backup files are out of the delete process
    |--------------------------------------------------------------------------
    */
   'storage_days' => env('DBACKUP_STORAGE_DAYS', 7),

    /*
    |--------------------------------------------------------------------------
    | Ignore the next database tables 
    |--------------------------------------------------------------------------
    */
    'ignore_tables' => []
];
